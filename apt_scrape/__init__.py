"""apt_scrape — Italian real estate listing scraper.

Public API surface is intentionally minimal; consumers should import
directly from ``apt_scrape.sites`` or run via ``apt_scrape.cli``.
"""

__version__ = "0.1.0"
